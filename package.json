{
  "name": "proyecto-viviana-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Proyecto Viviana SolidJS libraries",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/solid-stately build && bun run --cwd packages/solidaria build && bun run --cwd packages/solidaria-components build && bun run --cwd packages/silapse build",
    "build:web": "bun run --cwd apps/web build",
    "dev": "bun run --cwd apps/web dev",
    "start": "bun run --cwd apps/web start",
    "deploy": "bun run --cwd apps/web deploy",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "bun run --cwd apps/web bunx playwright test",
    "a11y:axe:aa": "RUN_AXE=1 bun run --cwd apps/web bunx playwright test e2e/playground-axe.spec.ts -g \"WCAG 2.2 AA\" --reporter=line",
    "a11y:axe:playground": "RUN_AXE=1 bun run --cwd apps/web bunx playwright test e2e/playground-axe.spec.ts --reporter=line",
    "a11y:smoke": "bun run --cwd apps/web bunx playwright test e2e/docs-menu.spec.ts e2e/playground-components.spec.ts e2e/calendar-regression.spec.ts --reporter=line",
    "a11y:check": "bun run a11y:axe:aa && bun run a11y:smoke",
    "a11y:full": "bun run a11y:axe:playground && bun run a11y:smoke",
    "a11y:ci": "bun run a11y:full",
    "typecheck": "deno task typecheck",
    "changeset": "changeset",
    "changeset:status": "changeset status --since=origin/main",
    "sync:manifest-versions": "node scripts/manifest-versions.mjs --fix",
    "check:manifest-versions": "node scripts/manifest-versions.mjs --check",
    "changeset:version": "changeset version && npm run sync:manifest-versions",
    "changeset:publish": "npm run build && changeset publish",
    "release": "npm run changeset:publish",
    "release:local": "npm run changeset:version && npm run changeset:publish"
  },
  "dependencies": {
    "brotli-size": "^4.0.0",
    "gzip-size": "^7.0.0"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.0",
    "axe-core": "^4.10.0",
    "@solidjs/testing-library": "^0.8.10",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/user-event": "^14.6.1",
    "@types/bun": "^1.3.5",
    "esbuild-plugin-solid": "^0.6.0",
    "jsdom": "^27.3.0",
    "rimraf": "^6.1.2",
    "solid-js": "^1.9.11",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vite-plugin-solid": "^2.11.10",
    "vitest": "^4.0.16"
  }
}
