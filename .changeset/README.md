# Changesets

This directory stores release notes and version bump intents for releasable packages.

- Add a changeset with `bun run changeset`.
- Version packages locally with `bun run changeset:version`.
- Publish packages with `bun run changeset:publish` (used by CI release workflow).
