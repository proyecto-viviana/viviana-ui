# Changesets

This directory stores release notes and version bump intents for releasable packages.

- Add a changeset with `npm run changeset`.
- Version packages locally with `npm run changeset:version`.
- Publish packages with `npm run changeset:publish` (used by CI release workflow).
