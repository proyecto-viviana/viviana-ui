# Changesets

This directory stores release notes and version bump intents for releasable packages.

- Add a changeset with `vp run changeset`.
- Version packages locally with `vp run changeset:version`.
- Publish packages with `vp run changeset:publish` (used by CI release workflow).
