# Changesets

This directory stores release notes and version bump intents for releasable packages.

- Add a changeset with `pnpm run changeset`.
- Version packages locally with `pnpm run changeset:version`.
- Publish packages with `pnpm run changeset:publish` (used by CI release workflow).
