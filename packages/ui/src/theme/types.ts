/**
 * Theme type definition for proyecto-viviana-ui
 */

export interface Theme {
  /** CSS class to apply to the root element. */
  className: string;
  /** CSS custom properties for the theme. */
  properties: Record<string, string>;
}
