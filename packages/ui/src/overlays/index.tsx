/**
 * Overlays module compatibility entrypoint for proyecto-viviana-ui.
 */

export {
  Dialog,
  DialogTrigger,
  DialogFooter,
} from '../dialog';
export type {
  DialogProps,
  DialogTriggerProps,
  DialogFooterProps,
  DialogSize,
} from '../dialog';

export {
  Popover,
  PopoverTrigger,
  PopoverHeader,
  PopoverFooter,
} from '../popover';
export type {
  PopoverProps,
  PopoverTriggerProps,
  PopoverHeaderProps,
  PopoverFooterProps,
  PopoverPlacement,
  PopoverSize,
  PopoverRenderProps,
} from '../popover';

export {
  Tooltip,
  TooltipTrigger,
  SimpleTooltip,
} from '../tooltip';
export type {
  TooltipProps,
  TooltipTriggerProps,
  TooltipPlacement,
  TooltipVariant,
  SimpleTooltipProps,
} from '../tooltip';

